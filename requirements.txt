# Boolean Circuit Editor — зависимости
# Python 3.10+

# GUI (раскомментировать один вариант)
PyQt6>=6.4
# tkinter — встроен в Python, отдельно не ставится

# Тесты
pytest>=7.0

# XML (stdlib xml.etree достаточно; для сложного разбора можно добавить)
# lxml>=4.9
